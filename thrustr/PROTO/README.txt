PROTO is all unfinished features stored away for later development
only use if you reallyyyy need to as there will NOT be any way to know if the features are all working correctly
you should only use features when they are done (or if you want to, you can see these features, but there is no gaurauntee it will be a working version)
these might have render.cs / main.cs files, these are for testing, you can use these to help use these while they're undocumented